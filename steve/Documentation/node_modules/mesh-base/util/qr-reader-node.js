const makeQrReader    = require('./qr-reader-factory');
const readImage       = require('./qr-reader-source-reader-node');
const buffferObject   = Buffer;

//..............................................................................
const {util} = cxq;
//..............................................................................

//..............................................................................
const {addProperties} = util;
//..............................................................................

addProperties(util,
{
    readQrImage: makeQrReader(buffferObject, readImage)
});