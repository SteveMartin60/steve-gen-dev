const
{
    getCachedHashedFile,
    uploadHashedFile,
    downloadHashedFile,
    getHashedFile,
    getFileHash,
} = require('./cached-assets');

const {addSecureAsset} = require('./secure-add');
const {getSecureAsset} = require('./secure-get');
const {addPlainAsset } = require('./plain-add');
const {getPlainAsset } = require('./plain-get');

//..............................................................................
const {cloud, util} = cxq;
//..............................................................................

//..............................................................................
const
{
    addProperties,
} = util;
//..............................................................................

addProperties(cloud,
{
    getCachedHashedFile,
    uploadHashedFile,
    downloadHashedFile,
    getHashedFile,
    getFileHash,
    addSecureAsset,
    getSecureAsset,
    addPlainAsset,
    getPlainAsset,
});
